import { VERSION } from '@env/version';

export interface AframeSettings {
}

export const geo_default_settings: AframeSettings = {
};

export const Locale = [{
    name: 'Singapore',
    zone: 'Asia/Singapore',
    lat: '1.35',
    long: '103.8'
}];
